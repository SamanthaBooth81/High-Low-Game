/**
 * The main game loop which is called when a game first starts 
 */
function runGame() {

    document.getElementById("random-number").value= "";
    // Generates the random number the user will make their guess from
    let randomNumber = Math.floor(Math.random) * 25;  
}

function checkAnswer()

function calculateCorrectAnswer()
